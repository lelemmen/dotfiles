### Defining shortcuts for commands

## Shortcuts
alias g='git'

## I'd like to use everything when in sudo on my Mac. The default shell for root is /bin/sh, which will first attempt to read and execute from /etc/profile (https://unix.stackexchange.com/a/195239).
alias sudo='sudo '              # If the last character of an alias value is a space or tab, then the next word is also checked for alias expansion (https://askubuntu.com/a/22043)



## Small commands
alias resource="source ~/.bash_profile"


## Give a better version of ls
# -G: enable colorized output (equivalent to export CLICOLOR=1)
# -F: better characterized output (/ after directories, * after executable, @ after symbolic link)
# -h: better readable: use kB, MB, ...
# -a: all
alias ls='ls -GFha'

## Directory navigation
alias ..="cls .." 			# Move up one directory
alias ...="cls ../.." 			# Move up two directories
alias ....="cls ../../.." 		# Move up three directories

alias godot="cls ${DOTFILES_DIR}"			# Go to the dotfiles directory
alias gotmp="cls ${PERS_TEMP_DIR}"			# Go to the personal temporary directory
alias gosoft="cls ${SOFTWARE_DIR}"			# Go to the software directory
alias gobin="cls ${SOFTWARE_BIN_DIR}"			# Go to my personal binary directory
alias gotop="cls /"					# Go to the root directory
alias gotype="cls ${DOCT_TYPE}"				# Go to my phd typesetting directory
alias gohome="cls ~"					# Go to ${HOME}
alias godoct="cls ~/Documents/Projecten/Doctoraat"	# Go to my phd project directory 
alias gofin="cls ~/Documents/Projecten/Persoonlijk/Beleggen\ en\ Financiën"
